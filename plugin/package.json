{
  "name": "zotero-research-assistant",
  "version": "0.1.0",
  "description": "Semantic search and AI-powered research assistant for Zotero",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:dev": "node scripts/build.mjs --dev",
    "start": "node scripts/build.mjs --dev --watch",
    "package": "node scripts/package.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "archiver": "^6.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
